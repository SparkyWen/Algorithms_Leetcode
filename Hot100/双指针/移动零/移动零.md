# 移动零

## 1. 知识点

> 1. 思路：
>
> 两次遍历
> 我们创建两个指针 i 和 j，第一次遍历的时候指针 j 用来记录当前有多少 非0 元素。即遍历的时候每遇到一个 非0 元素就将其往数组左边挪，第一次遍历完后，j 指针的下标就指向了最后一个 非0 元素下标。
> 第二次遍历的时候，起始位置就从 j 开始到结束，将剩下的这段区域内的元素全部置为 0。
>
> 先把位置往前面挤，即复制过去前面的位置，然后再通过j去处理一直到末尾的0

==所以什么是双指针思想：一个指针去遍历所有的，另一个指针初始化以后有着其他的操作。比如说这里的j，先初始化为0之后但是在循环之前不做其他的处理==

> ![](https://pic.leetcode-cn.com/9669b4ffb158eaeeee6f0cd66a70f24411575edab1ab8a037c4c9084b1c743f5-283_1.gif)
>
> 2. 快排思想：
>
> 这里参考了快速排序的思想，快速排序首先要确定一个待分割的元素做中间点 x，然后把所有小于等于 x 的元素放到 x 的左边，大于 x 的元素放到其右边。
> 这里我们可以用 0 当做这个中间点，把不等于 0(注意题目没说不能有负数)的放到中间点的左边，等于 0 的放到其右边。
> 这的中间点就是 0 本身，所以实现起来比快速排序简单很多，我们使用两个指针 i 和 j，只要 nums[i]!=0，我们就交换 nums[i] 和 nums[j]
>
> ![](https://pic.leetcode-cn.com/36d1ac5d689101cbf9947465e94753c626eab7fcb736ae2175f5d87ebc85fdf0-283_2.gif)

3. xrange（）用法：

**Python 2 里**：`range` 和 `xrange` 都存在，**区别很大**。

**Python 3 里**：**已经没有 `xrange` 了**，`range` 的行为变成了“Python2 的 `xrange`”。

`range(10000000)`：**先在内存里创建一个有一千万个整数的列表**，非常占内存。

`xrange(10000000)`：**不创建列表**，而是每次循环时“算出当前这个数字”，内存占用非常小。

“**只用来 for 循环一次**” → 优先用 `xrange`

“**需要这个列表本身，并多次用它**” → 用 `range`



## 2. 代码

### 2.1 两次遍历思路

```python
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        if not nums:
            return 0      # 这个其实可以不写，空数组时也不会出问题

        j = 0
        # 第一次遍历：把所有非 0 元素往前挤
        for i in range(len(nums)):   # 你截图里是 xrange，Python3 要用 range
            if nums[i]:             # 等价于 nums[i] != 0
                nums[j] = nums[i]
                j += 1

        # 第二次遍历：把后面的空位全部补 0
        for i in range(j, len(nums)):
            nums[i] = 0
```

逻辑是：

1. **第一次遍历**：
   - 遍历 `i = 0..n-1`
   - 如果 `nums[i]` 不是 0，就把它“压缩”到数组前面：
      `nums[j] = nums[i]`，然后 `j += 1`
   - 这样结束后，`nums[0..j-1]` 就是所有非 0 元素，顺序保持不变。
2. **第二次遍历**：
   - 从 `j` 开始，到数组末尾，把这些位置全部写成 `0`
   - 即 `nums[j..n-1]` 都变成 0。

这就实现了“把所有 0 挪到数组末尾，非 0 的相对顺序不变”。

```
用一个具体例子走一遍：[0, 1, 0, 3, 12]
初始状态
nums = [0, 1, 0, 3, 12]
j = 0

第一次 for 循环：for i in range(len(nums))
✅ i = 0

当前元素：nums[0] = 0

判断：if nums[0]: → if 0: 为 False

所以这一步什么都不做，j 还是 0

数组保持不变：

i = 0, j = 0
nums = [0, 1, 0, 3, 12]

✅ i = 1

当前元素：nums[1] = 1

判断：if nums[1]: → if 1: 为 True（非 0 就是 True）

进入 if：

nums[j] = nums[i]   # nums[0] = nums[1] = 1
j += 1              # j 由 0 变成 1


数组变化：

原来 nums = [0, 1, 0, 3, 12]
这一步后 nums = [1, 1, 0, 3, 12]   # 把 1 挤到了最前面
i = 1, j = 1


注意：我们是 覆盖 nums[0]，把里面的 0 替成了 1，相当于“把 1 往左挪”。

✅ i = 2

当前元素：nums[2] = 0

if nums[2]: → if 0: 为 False

不做任何事情，j 仍然是 1

i = 2, j = 1
nums = [1, 1, 0, 3, 12]

✅ i = 3

当前元素：nums[3] = 3

if nums[3]: → True

进入 if：

nums[j] = nums[i]   # nums[1] = nums[3] = 3
j += 1              # j: 1 -> 2


数组变化：

原 nums = [1, 1, 0, 3, 12]
变 nums = [1, 3, 0, 3, 12]
i = 3, j = 2


这一步就是把第二个非零数 3 挤到下一个位置 nums[1]。

✅ i = 4

当前元素：nums[4] = 12

if nums[4]: → True

进入 if：

nums[j] = nums[i]   # nums[2] = nums[4] = 12
j += 1              # j: 2 -> 3


数组变化：

原 nums = [1, 3, 0, 3, 12]
变 nums = [1, 3, 12, 3, 12]
i = 4, j = 3

第一次循环结束后的状态
nums = [1, 3, 12, 3, 12]
j = 3


这意味着：

nums[0], nums[1], nums[2] 是我们所有的非 0 元素（1、3、12），顺序保持原有顺序；

后面的 nums[3], nums[4] 已经不重要了，里面是“旧数据”（还没被归零的部分）。

接下来第二个循环要做的就是：把 j 之后的全部设为 0。

第二次 for 循环：for i in range(j, len(nums))

此时 j = 3, len(nums) = 5，所以 i 依次为 3, 4。

✅ i = 3
nums[3] = 0


数组变成：

nums = [1, 3, 12, 0, 12]

✅ i = 4
nums[4] = 0


数组变成：

nums = [1, 3, 12, 0, 0]

最终结果
nums = [1, 3, 12, 0, 0]


这就是题目要求的“把所有 0 移到末尾，保持非 0 元素的相对顺序不变”。
```

### 2.2 快排思路

```python
class Solution(object):
	def moveZeroes(self, nums):
		"""
		:type nums: List[int]
		:rtype: None Do not return anything, modify nums in-place instead.
		"""
		if not nums:
			return 0
		# 两个指针i和j
		j = 0
		for i in xrange(len(nums)):
			# 当前元素!=0，就把其交换到左边，等于0的交换到右边
			if nums[i]:
				nums[j],nums[i] = nums[i],nums[j]
				j += 1
```

